# IronTracker
An offline workout progressive web application to track and visualize workout details.

# Development
1. Must create a base first before running docker compose up with the tag 'iron-base'

    ``docker build -t iron-base .``
2. Start development environment with

    ``docker compose up -d``
